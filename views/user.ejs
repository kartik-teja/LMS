<!DOCTYPE html>
<html lang="en">

<head>
    <title>
        Next Move
    </title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/looks/output.css" rel="stylesheet">
</head>

<body>
    <h1>Welcome John Doe!</h1>
    <p>The following courses are available. Pick what you would like to learn today!</p>
</body>
    <div>
        <% courses.forEach(course => { %>
            <div class="bg-white p-6 rounded-lg shadow-md cursor-pointer" onclick="redirectToCourse('<%= course.dataValues.name %>')">
                <h2 class="text-xl text-gray-900 font-semibold mb-2"><%= course.dataValues.name %></h2>
                <p class="text-gray-700">Number of Chapters: <%= course.dataValues.chapters.length %></p>
            </div>
            <% }); %>
    </div>

</html>